extends ../layouts/application.pug

block header
  = t('views.session.new.signIn')

block content
  +formFor()(action=route('session') method='post')
    +input(signInForm, t('entities.user.email'), 'email', errors)(type="text")
    +input(signInForm, t('entities.user.password'),'password')(type="password")
    +submit(signInForm)(value=t('views.session.new.submit'))
